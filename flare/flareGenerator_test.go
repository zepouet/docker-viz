package viz

import (
	"testing"
	"github.com/stretchr/testify/assert"
)

func init() {
	// Delete all containers
	// Pull busybox
	// Generate many containers and commit new images for a tree
}

func TestCreateEngine(t *testing.T) {
	assert.Equal(t, 2, 2)
}